class AddOrderModel:


    def __init__(self, testId, product, quantity, observations, price,expectedResult):
        self.id = testId
        self.product = product
        self.quantity = quantity
        self.observations = observations
        self.price = price
        self.expectedResult = expectedResult
